[package]
name = "corpus"
version = "0.1.0"
edition.workspace = true
description = "Shared Ahamkara corpus model, loader, canonical constants, and validation checks"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
