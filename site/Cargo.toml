[package]
name = "sitegen"
version = "0.1.0"
edition = "2021"
description = "Static site generator for the Ahamkara archive — reads the lore corpus and emits dist/"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 2
